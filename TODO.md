# Tomorrow

## Delivery to finish

- After explicit public visibility approval: enable GitHub Pages using Actions, dispatch the Pages workflow, verify the served HTML and the browser demo, then close the Done milestone when all checks pass.

## Optional follow-ups



- Check the real-device experience in Safari and Firefox, including imports, drag/keyboard arrangement, downloads, storage recovery and guide focus on small screens.
- Add more irregular collage cases: unequal tiles, borders, mixed aspect ratios, large gutters and partial rows. Verify preview, correction, selection and imported pixels.
- Consider full-quality versions of the approved unposted generated samples for an export-quality demonstration. Record their actual dimensions and file sizes; keep actual posted photos excluded.

Keep a JSON layout backup before testing updates. These follow-ups extend coverage and sample quality; they do not add Instagram publishing or cloud sync.
